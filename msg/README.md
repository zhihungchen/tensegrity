# Deprecated for ROS builds

For ROS, use **tensegrity_interfaces** in `ros_ws/src/tensegrity_interfaces/` as the single source of message definitions. Source your `ros_ws` devel/setup and import from `tensegrity_interfaces.msg`.
